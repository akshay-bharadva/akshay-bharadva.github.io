import SecuritySettings from "@/components/admin/security-settings";
import { withAdminPage } from "@/components/admin/withAdminPage";

export default withAdminPage(SecuritySettings, { title: "Security Settings" });
