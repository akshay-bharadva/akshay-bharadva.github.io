import InventoryManager from "@/components/admin/inventory-manager";
import { withAdminPage } from "@/components/admin/withAdminPage";

export default withAdminPage(InventoryManager, { title: "Inventory" });
