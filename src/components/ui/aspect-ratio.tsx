/*
This component is a primitive from Radix UI and doesn't contain styling itself. The comment is updated for consistency with the redesign project. No functional or style changes are needed here as its styling is applied where it's used.
*/
"use client";

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
